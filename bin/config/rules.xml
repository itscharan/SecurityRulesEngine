<?xml version="1.0" encoding="UTF-8"?>
<import class="java.util.Calendar"/>


	<rule name="dividendRule">
		<condition>
			equity.nextDividendDate.
		</condition>
		<action>
			error = "dividend date is within a week"
		</action>

	</rule>

	<rule name="couponRule">
		<condition>
			bond.nextCouponDate.
		</condition>
		<action>
			error = "coupon date is within a week"
		</action>

	</rule>

	<rule name="issuerRule">
		<condition>
			sdi.issuer == "MUNICIPAL";
		</condition>
		<action>
			error = "Issuer not in the issued list"
		</action>

	</rule>